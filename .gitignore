# Fixtures = HTML real da conta. Contem nomes e coordenadas de TODAS as aldeias,
# contagem de tropa por aldeia e comandos em rota. O repositorio e PUBLICO —
# subir isto e entregar inteligencia militar pro inimigo. Ficam so na maquina.
fixtures/
